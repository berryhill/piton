# Piton evidence directory

Retained evidence packets bind task/session, exact base/head, revision, BuildAttempt, toolchain/environment, checks, tolerances, artifact hashes, CI IDs, human review, merge/install readback, and residual risk.

Do not store credentials or secret values. Generated evidence files remain untracked unless a reviewed task explicitly adds an immutable fixture.
